#include <iostream>

using namespace std;

int main(){
    
    int a, b;
    cout << "Enter the two integer numbers..." << endl;
    cin >> a >> b;

    float k;
    cin >> k;

    cout << "(a + b) * k = " << (a + b) * k << endl;

    return 0;
}