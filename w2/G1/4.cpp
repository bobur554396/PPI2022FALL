#include <iostream>

using namespace std;

int main(){

    /*
    - [ ] comparison operators (==, !=, >, <, >=, <=)
    */

    cout << (2 == 3) << endl; // equal?
    cout << (2 != 3) << endl; // not equal?
    cout << (2 < 3) << endl; // not equal?
    
    return 0;
}