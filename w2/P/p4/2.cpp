#include <iostream>

using namespace std;

int main(){
    // Conditional statements

    /*  

        if(condition) {
            instructions/expressions of "if" block
        } else {
            instructions/expressions of "else" block
        }

    */
    int a, b;
    cin >> a >> b;
    // 14 10 

    if(a > b){ // 14 > 10
        cout << a << endl;
    } else {
        cout << b << endl;
    }


    return 0;
}