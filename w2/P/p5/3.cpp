#include <iostream>

using namespace std;

int main(){
    // Conditional statements

    /* 

    if(condition){
        instructions/exression of "if" block
    } else {
        instructions/exression of "else" block
    }
    */
    // 2 3
    // 3
    // 14 10
    // 14
    int a, b;
    cin >> a >> b;

    if(a > b){ // 4 > 8
        cout << a << endl;
    } 

    if(b > a) {
        cout << b << endl;
    }



    return 0;
}