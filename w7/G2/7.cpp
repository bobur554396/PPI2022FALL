#include <iostream>

using namespace std;

int sum(int n, int m){
    int res = n + m; // local variables
    return res;
}

int main(){
    int a, b;
    cin >> a >> b;

    // int r = sum(a, b);
    // // cout << r << endl;
    // cout << sum(a, b) << endl;

    return 0;
}